# Architecture

The browser has three small boundaries: deterministic scenario inputs, the pure `classify` function, and the `advance` state transition. React renders the current engine snapshot. localStorage restores the event history and impact metrics. The firmware duplicates the same thresholds in a platform-independent C++ classifier and emits telemetry over serial.
