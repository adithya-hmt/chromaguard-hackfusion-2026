# Hardware integration

Use conditioned, isolated sensor interfaces and a protected MOSFET/relay driver for the 12 V solenoid. Add a normally-safe valve selection, tank-level switch, watchdog, manual emergency stop, fusing, flyback suppression, and independent plant interlock. The firmware pin map is in `firmware/chromaguard/config.h`.
