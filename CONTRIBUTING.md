# Contributing

Keep classifier changes mirrored in `src/simulation/classifier.ts` and `firmware/chromaguard/classifier.cpp`, add a deterministic test for rule changes, and run `npm run typecheck && npm test -- --run && npm run build` before opening a pull request.
